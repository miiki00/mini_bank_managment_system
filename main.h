#ifndef MAIN_H
#define MAIN_H

/* standard libraries */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* local defination and declarations */
int _check_pass(char *str);
void _help_message(void);
int add (char *name);
int rem (unsigned int id);
int info(unsigned int id);

#endif
